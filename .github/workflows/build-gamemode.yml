name: Build GameMode

on:
  push:
    branches: [main]
    paths:
      - 'build-scripts/build-gamemode.sh'
      - 'build-scripts/common.sh'
      - 'debian/gamemode/**'
  workflow_dispatch:
    inputs:
      version:
        description: 'Upstream version/tag to build (leave empty for latest)'
        required: false
        default: ''

jobs:
  build:
    uses: ./.github/workflows/build-package.yml
    with:
      package_name: gamemode
      version: ${{ inputs.version || '' }}
    permissions:
      contents: write
