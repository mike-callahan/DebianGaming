name: Build MangoHud

on:
  push:
    branches: [main]
    paths:
      - 'build-scripts/build-mangohud.sh'
      - 'build-scripts/common.sh'
      - 'debian/mangohud/**'
  workflow_dispatch:
    inputs:
      version:
        description: 'Upstream version/tag to build (leave empty for latest)'
        required: false
        default: ''

jobs:
  build:
    uses: ./.github/workflows/build-package.yml
    with:
      package_name: mangohud
      version: ${{ inputs.version || '' }}
    permissions:
      contents: write
