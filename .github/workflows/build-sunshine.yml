name: Build Sunshine

on:
  push:
    branches: [main]
    paths:
      - 'build-scripts/build-sunshine.sh'
      - 'build-scripts/common.sh'
      - 'debian/sunshine/**'
  workflow_dispatch:
    inputs:
      version:
        description: 'Upstream version/tag to build (leave empty for latest)'
        required: false
        default: ''

jobs:
  build:
    uses: ./.github/workflows/build-package.yml
    with:
      package_name: sunshine
      version: ${{ inputs.version || '' }}
    permissions:
      contents: write
