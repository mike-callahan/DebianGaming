#!/usr/bin/make -f

export DEB_BUILD_OPTIONS = nocheck

%:
	dh $@

override_dh_auto_configure:
	dh_auto_configure --buildsystem=meson -- \
		--wrap-mode=nodownload

override_dh_auto_build:
	dh_auto_build --buildsystem=meson --parallel=1
